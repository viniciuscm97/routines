# Projeto para executar rotinas

Projeto criado utilizando Node JS, express que executa funções após um periodo definido.


# Como iniciar
- Instalar módulos

    ```bash
    npm install
    ```

- Executar projeto

    ```bash
    npm run dev
    ```
